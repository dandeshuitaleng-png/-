# entry/src/main/ets/config/FeatureFlags.ets

```typescript
// Keep the real location flow available while emulator testing uses a stable mock position.
export const ENABLE_DEVICE_LOCATION_ON_STARTUP: boolean = false;

```
